/// @addtogroup grp_version
/// @{
///
/// @file  version.h
///
/// Version header file
///
/// Read by build_flash_tools/run_build_tool.py to name the artefact it copies
/// into output/. Bump it in the same commit as whatever change earns the bump -
/// a version that moves separately from the code names nothing.

#pragma once

#define VER_MAJOR 0
#define VER_MINOR 1
#define VER_PATCH 0
#define RELEASE_TAG "dev"

/// @}
